import mapStyle from "@/assets/mapStyle.json";

export default {
  mapStyle: mapStyle,
  view: {
    center: [83.3, 42.2],
    zoom: 13,
    pitch: 0,
    bearing: 0,
  },
  geoJsonOptions: {},
  minZoom: 0,
  maxZoom: 22,
};
